# Grid Layout Tasks

- [x] **T01**: Define `WidgetPlacement` entity with `gridX`, `gridY`, `gridWidth`, `gridHeight` integer columns and tile fields — `mydash/lib/Db/WidgetPlacement.php`
- [x] **T02**: Define `GridPositionInterface` with typed getters/setters for grid coordinates — `mydash/lib/Db/GridPositionInterface.php`
- [x] **T03**: Implement `WidgetPlacementMapper` with `findByDashboardId` (ordered by `sort_order`, `grid_y`, `grid_x`), `updatePositions` batch updater, and `deleteByDashboardId` — `mydash/lib/Db/WidgetPlacementMapper.php`
- [x] **T04**: Implement `PlacementService` with `addWidget`, `addTileFromArray`, `updatePlacement`, `removePlacement`, `getDashboardPlacements` — `mydash/lib/Service/PlacementService.php`
- [x] **T05**: Implement `PlacementUpdater` and `TileUpdater` helper services for applying grid and tile field updates to a placement entity — `mydash/lib/Service/PlacementUpdater.php`, `mydash/lib/Service/TileUpdater.php`
- [x] **T06**: Implement `DashboardService.updateDashboard` with `applyDashboardUpdates` that handles the `placements` key by calling `WidgetPlacementMapper.updatePositions` — `mydash/lib/Service/DashboardService.php`
- [x] **T07**: Register dashboard API routes (`GET /api/dashboards`, `GET /api/dashboard`, `POST /api/dashboard`, `PUT /api/dashboard/{id}`, `DELETE /api/dashboard/{id}`, `POST /api/dashboard/{id}/activate`) — `mydash/appinfo/routes.php`
- [x] **T08**: Register widget placement routes (`POST /api/dashboard/{dashboardId}/widgets`, `POST /api/dashboard/{dashboardId}/tile`, `PUT /api/widgets/{placementId}`, `DELETE /api/widgets/{placementId}`) — `mydash/appinfo/routes.php`
- [x] **T09**: Implement `DashboardApiController` with `list`, `getActive`, `create`, `update`, `delete`, `activate` actions and permission checks via `PermissionService` — `mydash/lib/Controller/DashboardApiController.php`
- [x] **T10**: Implement `WidgetApiController` with `addWidget`, `addTile`, `updatePlacement`, `removePlacement` actions — `mydash/lib/Controller/WidgetApiController.php`
- [x] **T11**: Define `Dashboard` entity with `gridColumns` (default 12), `permissionLevel` (`full` / `view_only` / `add_only`), `isActive`, `type` (`user` / `admin_template`) — `mydash/lib/Db/Dashboard.php`
- [x] **T12**: Implement `DashboardStore` (Pinia) with `widgetPlacements`, `activeDashboard`, `permissionLevel` state and `loadDashboards`, `switchDashboard`, `updatePlacements`, `addWidgetToDashboard`, `addTileToDashboard`, `removeWidgetFromDashboard`, `updateWidgetPlacement` actions — `mydash/src/stores/dashboard.js`
- [x] **T13**: Add `api.js` service layer with `getDashboards`, `getActiveDashboard`, `updateDashboard`, `addWidget`, `addTile`, `updateWidgetPlacement`, `removeWidget` using `@nextcloud/axios` — `mydash/src/services/api.js`
- [x] **T14**: Build `DashboardGrid.vue` — initialise GridStack with `column`, `cellHeight: 80`, `margin: 12`, `float: true`, `animate: true`; bind `disableDrag`/`disableResize` from `editMode` prop — `mydash/src/components/DashboardGrid.vue`
- [x] **T15**: Implement `DashboardGrid.initGrid()` using `GridStack.init()` targeting the inner `.grid-stack` element, attach `change` event listener that calls `handleGridChange` — `mydash/src/components/DashboardGrid.vue`
- [x] **T16**: Implement `DashboardGrid.handleGridChange(items)` — map changed GridStack nodes to updated placement objects and emit `update:placements` — `mydash/src/components/DashboardGrid.vue`
- [x] **T17**: Implement `DashboardGrid.syncGridItems(placements)` — add missing items via `grid.makeWidget` in `$nextTick`, remove stale items via `grid.removeWidget(el, false)` — `mydash/src/components/DashboardGrid.vue`
- [x] **T18**: Implement `editMode` watcher in `DashboardGrid` calling `grid.enable()` / `grid.disable()` to toggle drag/resize without reinitialising — `mydash/src/components/DashboardGrid.vue`
- [x] **T19**: Implement `getPlacementKey` returning a composite string (`id-updatedAt-styleConfigJSON`) to force item re-render on style or timestamp changes — `mydash/src/components/DashboardGrid.vue`
- [x] **T20**: Implement `isTilePlacement` detection (`tileType === 'custom'`) and `getTileData` extractor to route placements to `TileWidget` vs `WidgetWrapper` — `mydash/src/components/DashboardGrid.vue`
- [x] **T21**: Add `beforeDestroy` cleanup calling `grid.destroy(false)` to prevent GridStack memory leaks on dashboard switch — `mydash/src/components/DashboardGrid.vue`
- [x] **T22**: Build `Views.vue` with `isEditMode` local state, floating controls (Cog/Close toggle + Add button), `canEdit` computed from `permissionLevel`, and `toggleEditMode` method that closes picker and style editor — `mydash/src/views/Views.vue`
- [x] **T23**: Wire `Views.vue` `@update:placements="updatePlacements"` to `DashboardStore.updatePlacements()` for optimistic local state update + API save — `mydash/src/views/Views.vue`
- [x] **T24**: Implement `DashboardStore.updatePlacements` — immediately write `this.widgetPlacements = placements` then call `api.updateDashboard(id, { placements: [...] })` with `{ id, gridX, gridY, gridWidth, gridHeight }` per placement — `mydash/src/stores/dashboard.js`
- [x] **T25**: Build `WidgetWrapper.vue` — flex-column layout with conditional header (icon + title + Cog action in edit mode), content area, and footer buttons; apply `styleConfig` (backgroundColor, border, borderRadius, padding, headerStyle) as computed inline styles — `mydash/src/components/WidgetWrapper.vue`
- [x] **T26**: Build `TileWidget.vue` — absolutely positioned tile filling the grid cell; render SVG/class/emoji/URL icon with `--tile-bg-color` / `--tile-text-color` CSS variables; show settings button overlay in `editMode`; inject per-tile `<style>` on `mounted` to override nldesign CSS — `mydash/src/components/TileWidget.vue`
- [x] **T27**: Build `WidgetPicker.vue` — slide-in panel (320px, `transform: translateX`) with Widgets/Dashboards tabs; widget search + sort (unplaced first); tile creation and dashboard CRUD actions — `mydash/src/components/WidgetPicker.vue`
- [x] **T28**: Build `WidgetRenderer.vue` — route to `NcDashboardWidget` (API v1/v2), legacy mount container, or `TileWidget`; setup store subscription for reactive item updates; implement auto-refresh via `setInterval`; use exponential-backoff retry for legacy widget callback mounting — `mydash/src/components/WidgetRenderer.vue`
- [x] **T29**: Add GridStack CSS (`gridstack/dist/gridstack.min.css`) and scoped styles for `.mydash-grid`, transparent `.grid-stack`, item content background with `var(--color-main-background)`, conditional border for `.mydash-widget` cells, and primary-colored drag placeholder — `mydash/src/components/DashboardGrid.vue`
- [x] **T30**: Add `gridstack: "^10.3.1"` to `package.json` dependencies — `mydash/package.json`
