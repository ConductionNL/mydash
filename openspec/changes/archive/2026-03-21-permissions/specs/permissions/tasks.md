# Permission Tasks

- [x] **T01**: Define `Dashboard` entity permission level constants and default — `lib/Db/Dashboard.php`
- [x] **T02**: Define `WidgetPlacement` entity `is_compulsory` flag (SMALLINT 0/1) — `lib/Db/WidgetPlacement.php`
- [x] **T03**: Add `permission_level` column (VARCHAR 20, default `'full'`) to `mydash_dashboards` DB schema — `lib/Migration/DashboardTableBuilder.php`
- [x] **T04**: Add `is_compulsory` column (SMALLINT UNSIGNED, default `0`) to `mydash_widget_placements` DB schema — `lib/Migration/PlacementTableBuilder.php`
- [x] **T05**: Implement `PermissionService` with `canEditDashboard`, `canAddWidget`, `canRemoveWidget`, `canStyleWidget`, ownership verification, and `getEffectivePermissionLevel` (template-override → own level → global default) — `lib/Service/PermissionService.php`
- [x] **T06**: Implement `PermissionService::canCreateDashboard` and `canHaveMultipleDashboards` gated by admin settings — `lib/Service/PermissionService.php`
- [x] **T07**: Enforce `canAddWidget` check (HTTP 403) on `POST /api/dashboard/{dashboardId}/widgets` — `lib/Controller/WidgetApiController.php`
- [x] **T08**: Enforce `canAddWidget` check (HTTP 403) on `POST /api/dashboard/{dashboardId}/tile` — `lib/Controller/WidgetApiController.php`
- [x] **T09**: Enforce `canStyleWidget` check (HTTP 403) on `PUT /api/widgets/{placementId}` — `lib/Controller/WidgetApiController.php`
- [x] **T10**: Enforce `canRemoveWidget` check (HTTP 403) on `DELETE /api/widgets/{placementId}`, including compulsory-widget block for `add_only` — `lib/Controller/WidgetApiController.php`
- [x] **T11**: Enforce `canEditDashboard` check (HTTP 403) on `PUT /api/dashboard/{id}` — `lib/Controller/DashboardApiController.php`
- [x] **T12**: Enforce `canCreateDashboard` and `canHaveMultipleDashboards` checks (HTTP 403) on `POST /api/dashboard` — `lib/Controller/DashboardApiController.php`
- [x] **T13**: Ensure `DELETE /api/dashboard/{id}` bypasses permission level check (ownership only) — `lib/Controller/DashboardApiController.php`
- [x] **T14**: Enforce placement ownership check (HTTP 403 / exception) on rule read/create endpoints — `lib/Controller/RuleApiController.php`
- [x] **T15**: Set `permission_level` to `PERMISSION_FULL` on all user-created dashboards in `DashboardFactory` — `lib/Service/DashboardFactory.php`
- [x] **T16**: Copy `permission_level` from template to user dashboard copy in `TemplateService::buildDashboardFromTemplate` — `lib/Service/TemplateService.php`
- [x] **T17**: Copy `is_compulsory` flag verbatim from template placement to user placement copy in `TemplateService::clonePlacement` — `lib/Service/TemplateService.php`
- [x] **T18**: Force `PERMISSION_VIEW_ONLY` when serving admin template directly (no user copy, `allow_user_dashboards = false`) in `DashboardResolver::handleTemplateResult` — `lib/Service/DashboardResolver.php`
- [x] **T19**: Implement `DashboardResolver::getEffectivePermissionLevel` (template override → own level → global default) and surface `permissionLevel` in the `GET /api/dashboard` response payload — `lib/Service/DashboardResolver.php`
- [x] **T20**: Exclude `permission_level` from user-editable fields in `DashboardService::applyDashboardUpdates` — `lib/Service/DashboardService.php`
- [x] **T21**: Exclude `is_compulsory` from user-editable fields in `PlacementUpdater::applyDisplayUpdates` — `lib/Service/PlacementUpdater.php`
- [x] **T22**: Always set `is_compulsory = 0` on user-initiated widget and tile placements in `PlacementService` — `lib/Service/PlacementService.php`
- [x] **T23**: Default template `permissionLevel` to `PERMISSION_ADD_ONLY` in `AdminTemplateService::createTemplate` — `lib/Service/AdminTemplateService.php`
- [x] **T24**: Define `AdminSetting::KEY_DEFAULT_PERMISSION_LEVEL` constant and wire fallback in `PermissionService` — `lib/Db/AdminSetting.php`
- [x] **T25**: Gate the Edit/Customize toggle button on `canEdit` (i.e. `permissionLevel !== 'view_only'`) in the main view — `src/views/Views.vue`
- [x] **T26**: Conditionally show widget configure (Cog) button only in `editMode` — `src/components/WidgetWrapper.vue`
- [x] **T27**: Compute `canRemove` from `placement.isCompulsory` on the widget wrapper to conditionally hide remove action — `src/components/WidgetWrapper.vue`
- [x] **T28**: Wire GridStack `disableDrag`/`disableResize` to `editMode` and toggle via `grid.enable()`/`grid.disable()` on mode change — `src/components/DashboardGrid.vue`
- [x] **T29**: Expose `permissionLevel` from the dashboard store to the main view via Pinia state — `src/views/Views.vue`
